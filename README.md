# I Ching Daily Reflection 
 
Quick start: run `docker-compose up -d` then `cd apps/api && npm install && npm run start:dev`. 
For mobile: `cd apps/mobile && flutter pub get && flutter run`. 
 
Modules: profiles, guidance, reflections, community, billing, notifications. 
 
See docs/product-safety.md for content and compliance guidelines. 
